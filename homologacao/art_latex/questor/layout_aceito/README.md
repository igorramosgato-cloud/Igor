# layout_aceito/

Coloque aqui o arquivo físico que o Questor **já aceitou/importou com
sucesso** (evidência primária de layout).

**Este arquivo NUNCA é versionado no Git** — ver
`.claude/rules/homologacao-dados.md`. Ele fica só localmente. O que entra no
repositório é o registro em `../evidencia/manifest.json` (nome, SHA-256,
competência, status) e o contexto em `../evidencia/README.md`.
